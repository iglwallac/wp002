import { resolve } from 'path'

export const MANIFEST_JSON_FILE_PATH = resolve(__dirname, 'manifest.json')
export const MANIFEST_IMG_192_FILE_PATH = resolve(
  __dirname,
  'img/homescreen192.png',
)
export const MANIFEST_IMG_512_FILE_PATH = resolve(
  __dirname,
  'img/homescreen512.png',
)
